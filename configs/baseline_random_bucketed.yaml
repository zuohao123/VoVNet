# Bucketed random policy baseline (entropy buckets).
# Update ratios once you measure VoVNet bucket ratios for the target lambda_cost.
policy:
  baseline_name: random_policy_matched
  baseline_seed: 0
  baseline_bucket_ratios:
    - [0.34, 0.33, 0.33]  # low entropy
    - [0.34, 0.33, 0.33]  # mid entropy
    - [0.34, 0.33, 0.33]  # high entropy
  baseline_bucket_thresholds: null
